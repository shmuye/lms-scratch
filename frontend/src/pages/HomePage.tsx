import { NavBar, Hero } from "../components";

const HomePage = () => {
  return (
    <div className="h-screen flex flex-col">
      <NavBar />
      <Hero />
    </div>
  );
};

export default HomePage;
